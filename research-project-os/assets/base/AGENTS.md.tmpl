# Repository Instructions

## Language

- 面向 human 的说明、handoff 和 HTML report 默认使用中文。
- 专业术语、code、paths、commands、IDs 和 machine-readable values 保持英文。

## Session bootstrap

- 在回答项目问题、设计分析、修改代码或执行命令前，必须先读取：
  1. `AGENTS.md`：稳定规则与安全边界；
  2. `QUESTIONS.md`：Project purpose、Input constraints、Output requirements、
     FAQ、Current question 与 Question queue；
  3. `CURRENT_HANDOFF.md`：当前 objective、已完成结果、outputs、blockers、
     interpretation boundary、next minimum action 与 resume commands；
  4. `project_manifest.yaml`：ownership、paths、Pixi 与 lifecycle 配置。
- 若存在当前 explore task，再读取该 task 的 `task.yaml`、`README.md` 与当前
  `report.md`；默认不加载历史 report、旧 receipts、handoff archive 或 archive
  snapshot 内容。
- 开始工作前先确认 current question、handoff 状态、I/O 限制和 stop condition。
  若上下文缺失、互相冲突或 current question 未明确，停止执行并请 human 决定。

## Context and ownership

- `QUESTIONS.md` 由 human 维护，Agent 与 CLI 只读；不得代替 human 填写、改写、
  重排或自动推进问题队列。
- `CURRENT_HANDOFF.md` 由 Agent 维护。恢复 session 时先按其 next minimum action
  继续；结束实质工作时通过 `close` 归档旧 handoff 并写入新的恢复入口。
- task/archive/release manifests、run receipts 和 lifecycle log 只由 CLI 写入。
- archive snapshot、released pipeline 与 released HTML 一经创建不得修改。

## Superpowers

- You may use superpowers, but do not write any spec or plan.
- superpowers 只能辅助当前已批准任务，不得创建 spec/plan 文件，也不得绕过
  `QUESTIONS.md` ownership、human approval、单问题推进、dry-run、audited `run`、
  handoff、Pixi 或安全限制。

## Exploration

- 一次只讨论或执行一个 current question；先讨论 inputs、method、outputs 和 stop
  condition，human 明确批准后才创建 task 或运行计算。
- 目录名使用 `P<order>-<core>-<short-english-summary>/`；scripts、derived、
  figures、runs 和 report 全部留在 task 内。
- 结果完成后停止并报告结论与限制，不自行修改 `QUESTIONS.md` 或开始下一题。
- explore code 按实际执行顺序线性组织，显示 intermediate objects、diagnostics、
  observations 和 limitations。允许少量重复，不提前创建 generic helpers、
  classes、wrappers、config layers 或跨文件 abstractions。
- 每个 executable code file 顶部写简短中文提纲，并使用对应编号中文 section
  headings；按 workflow 语义切分，不按固定行数切分。
- analysis scripts 只负责计算与 tables/figures；不得包含 HTML、CSS、模板或
  report rendering。报告使用独立 `report-build` API。
- 人工审核后才可 archive；pipeline 只从已验证 archive 重写，runtime 不得依赖
  `explore/` 或 `archive/`。

## Inputs, outputs, and environment

- 所有声明输入只读；实际分析通过 audited `rpos run` 执行并记录前后 SHA-256。
- 声明输出必须位于当前 task；formal report 必须是 `reports/*.html`。
- Pixi 只允许根 workspace 与根 `pixi.lock`；禁止嵌套 workspace、lock 和 `.pixi/`。
- 所有 mutation 先 dry-run，再显式 `--apply`；不得隐式 overwrite。
- 不自动运行 `git add`、`git commit`、`git push`、环境求解或外部同步。
- credentials、sensitive data、source data 和大型 generated objects 不得进入 Git。
