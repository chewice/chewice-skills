# Repository Instructions

## Language

项目说明默认使用中文。稳定的 engineering terms、machine-readable contract 和
文件名保持英文。

## Session Protocol

- 修改项目逻辑前，读取 `project_manifest.yaml`、`CURRENT_HANDOFF.md` 和
  `QUESTIONS.md`。
- 只加载 required context 和与当前 task 相关的 evidence。
- 项目状态发生实质变化时更新 handoff。
- You may use superpowers, but do not write any spec or plan.

## Evidence

- 区分 execution、observation、hypothesis、evidence 和 decision。
- 不得把 exploratory output 直接提升为 `verified` 或 `stable` 结论。
- 记录 inputs、validation、environment、exclusions、random seed、overlap 和
  limitations。

## Analysis Lifecycle

- 根目录 `QUESTIONS.md` 由 human 维护，Agent 默认只读。一次只讨论或执行其中
  唯一的 `Current question`；`Next questions` 不得提前分析。
- 对 `generic-analysis` 和 `bioinformatics`，先围绕当前问题讨论 method、
  expected outputs 和 stop condition；只有 `Human decision` 为
  `approved_to_run` 且 human 明确同意后，才创建目录或执行计算。
- 一次最多有一个未归档、未取消的 explore task；目录名使用
  `P<order>-<core>-<short-english-summary>/`，例如
  `P0-QC-low-quality-cells-removed/`。`P0` 表示唯一顺序，`QC`/`cluster`/`GRN`
  表示核心任务，末段是简短英文总结。
- 每个 explore task 的 scripts、derived data 和 figures 都留在自己的目录。
- 得到结果后停止，先向 human 报告答案、限制和候选下游问题；不得自行开始下一题
  或修改 `QUESTIONS.md`。
- 把 explore code 写成可执行 lab notebook：使用单个主 notebook、Quarto/R
  Markdown，或按实际运行顺序分 section/cell 的 script；在相邻位置保留
  intermediate objects、diagnostics、observations 和 limitations。
- 每个 explore 和 pipeline code file 顶部写简短中文提纲，并使用对应的编号中文
  section/cell headings。按有意义的 workflow steps 切分，通常 3–8 段；短脚本可
  更少，不按固定行数机械切段。code identifiers、paths、commands 和 keys 保持英文。
- 单次逻辑保持 inline，允许少量重复；不要过早创建 generic helpers、classes、
  wrappers、config layers 或跨文件 abstractions。仅对已稳定且重复的操作，或需要
  隔离风险的操作，提取短小且命名具体的函数。
- 只有人工审核后才能将完整 task 冻结为 `archive/<task>/vNNN/`；不得编辑或
  覆盖 snapshot，且入库不自动构成 verified evidence。
- 把模块化、参数化、复用接口和系统性 tests 留到 `pipeline/`。它只从获批
  archive 整理独立主流程；runtime 不得依赖 `explore/` 或 `archive/`，发表前经
  人工审核生成 release manifest。

## Safety

- 保留现有路径和 source data。
- Pixi 只使用根 workspace；子项目使用 features、environments、namespaced
  tasks 和 `task.cwd`。
- generated artifacts 必须通过显式 overwrite flag 才能覆盖。
- 除非用户明确要求，否则不得 commit、push 或写入 Notion。
- credentials、sensitive data 和大型 generated objects 不得进入 Git。
