# Task Register

尚未登记 task。添加 `TASK-001` 时需包含 status、priority、dependencies、
action 和 completion criteria。分析类 profile 的 explore task 另使用
`P<order>-<core>-<short-english-summary>/`，`P` 编号在项目中唯一并表示顺序；
每个 explore task 必须关联 `QUESTIONS.md` 中唯一的 current `Q-` ID。
