# Research Project Skill 重构方案

本方案用于指导 `research-context` Skill 的后续重构。本轮只完善方案文档，不修改 Skill 实现。

## 一、定位与边界

> 在用户已有的项目结构中，以研究问题组织工作，维护一个精简、可恢复的 `context.md`，并规范 Agent 之间的任务交接、证据核验和信息汇总。研究者保留方向与取舍的决定权。

本次重构明确移除自动构建脚手架的职责，包括项目初始化、自动创建控制文件和目录，以及依赖固定目录、编号和模板的记录生成要求。项目脚手架由用户人工构建。

新版只维护上下文摘要与交接规则：

- 项目只维护一个 `context.md`，不再要求根与局部 Handoff、Context Map 或其他分层上下文文件。
- 沿用用户指定的上下文位置；未另行指定时，已有的 `doc/context.md` 是约定入口。沿用已有格式，不要求迁移、重命名或创建第二份摘要。
- 入口缺失时不自动创建文件或目录。持久化入口由用户指定或建立；能够依据当前对话和现有资料开展的工作继续，明确尚未持久化的状态即可。
- 不要求 `QUESTIONS.md`、BRIEF/RESULT、Q-ID/A-ID、固定审核字段或专用记录工具。详细设计、分析过程与证据由项目已有文件承载，摘要提供必要入口。
- 不承担领域分析方法、环境构建、分析脚本风格、正式报告生成或完整科研审批流程。

后续实施应围绕此定位删除不再适用的旧职责，不受“最小必要修改、保留所有已有流程”的限制；保留问题驱动、按需读取、证据可追溯和人类决策等有效原则。移除 Skill 的结构要求不意味着删除或迁移用户已有项目文件。

## 二、问题驱动

每项实质工作应能说明：当前在回答什么问题，为什么需要这一步，什么观察会改变判断，以及下一步要解决什么。

- 保留用户原始目标，区分研究目标、当前子问题与 Agent 提出的解释。问题尚未确定时可以继续澄清和探索，不强制先形成假设。
- 从观察、数据生成过程、比较对象和必要假设出发，选择能够改变判断的最小分析。解释力与证据相当时，优先采用额外假设更少的解释。
- 重要任务开始前明确必要输入和适当的检查方式。成功完成分析不要求获得显著、支持性或符合预期的结果。
- 观察、解释与人的决定分别表达。事后发现及方法调整不冒充事前设计，但无需为此建立固定格式的记录文档。
- 新证据或用户修正可以改变问题、比较方式和下一步；不要只把修正附在原有路线后面。

工作可以从“明确问题 → 按需读取 → 执行与核验 → 理解观察 → 更新上下文”展开。委派、方案比较和请求人的决定只在需要时加入；任何一步都可以返回问题继续探索，不把它变成固定阶段序列。

## 三、上下文恢复与更新

### 恢复

遵循项目已有指令，读取唯一的 `context.md`，结合当前用户请求识别活动问题，再按摘要中的入口定向读取必要的数据说明、代码、产物或报告。入口缺失或过时时，围绕当前问题查找和核实，不为了了解项目默认递归读取整个仓库。

`context.md` 是压缩后的当前状态，不能替代被引用的证据。后续工作实际依赖某个结果时，应读取相应产物及必要说明，不仅依赖 Agent 的转述。

发现摘要与其他来源冲突时，先判断冲突属于哪一类：

- 实际输入、输出或检查结果：核对相关产物、代码及执行记录，区分当前版本与旧版本。
- 解释或推断：保留依据、假设和不确定性，不因文件较新就认定解释成立。
- 人的决定：以用户明确作出的决定为依据。产物或测试结果不能自行撤销研究取舍；新证据需要改变取舍时，提交用户判断。

报告影响当前工作的冲突并更新已核实的摘要；不能确认的内容标明待核实，不静默覆盖。

### 保存内容

沿用已有文档格式，以足够简短的内容回答：

| 内容 | 恢复工作时需要知道什么 |
| --- | --- |
| 目标与当前问题 | 用户要解决什么，现在具体追问什么 |
| 当前进展 | 做到了哪里，哪些事实已核实，哪些解释仍是暂定的 |
| 输入与产物入口 | 当前使用哪些文件或对象，必要的版本或处理状态是什么 |
| 关键决定 | 用户已决定什么、为什么；Agent 的建议是否仍待决定 |
| 未决问题与探索分支 | 有哪些限制、反例、冲突、待比较方案或阻塞 |
| 下一步 | 做什么、为什么做、需要先满足什么条件 |

这些是信息要求，不是强制模板或字段合同。简单项目无需空章节，大型项目也只维护这一份摘要，通过路径引用详细材料。

保留会影响后续判断的失败、反例、方向修正，以及方案被放弃的简短理由和重新考虑的条件。不要因某个结果不显著、与主解释冲突或暂时未被选择，就把它从可恢复的上下文中抹去。

原始终端日志、长篇调试过程、完整数据表、整段搜索结果、重复内容和无关尝试不写入摘要。已持久化的细节用入口替代；压缩信息时保留判断依据，不只留下结论。

### 更新时机与责任

输入或产物改变、获得重要观察、用户作出决定、研究方向调整、发现影响下游的问题时，由 Main Agent 更新发生变化的摘要内容，不逐条命令记录。

切换任务、中断、总结或交接前，保存尚未记录的重要进展、未完成工作及恢复入口。无实质变化时不重写；更新时注明时间，旧状态被取代后保留仍有决策价值的原因与引用。

Main Agent 统一维护 `context.md`。Sub-agent 返回建议更新项，不直接编辑共享摘要。用户已授权的任务内，维护已有摘要无需每次重复询问；不存在摘要入口时遵循前述不自动创建规则。

## 四、任务委派

Main Agent 负责目标理解、依赖协调、结果整合和与用户的交互，也可以直接执行任务。工具与授权允许，且委派确实有价值时，才使用 Sub-agent。

适合委派的工作包括：中间信息多但结论简短的定向调查、可独立进行的证据核查、输入可比较的候选方案评估，以及共享稳定输入且互不依赖的下游任务。

读取短文件、明确的小修改和简单检查通常直接完成。不要为每一步创建 Agent，不把有依赖的任务强行并行；没有 Sub-agent 时由 Main Agent 按相同原则继续。

### Main → Sub-agent

以简短说明交代以下内容，不要求额外合同文件或固定序列化格式：

- **问题与交付：** 回答哪个具体问题，需要返回什么。
- **输入：** 必要资料和产物路径，以及足以识别本次输入的版本或处理状态。
- **边界：** 允许读取和修改的范围、输出位置、不得改变的上游条件与用户决定。
- **核验：** 怎样检查工作是否正确完成，哪些条件不足时应报告未完成或不确定。

只提供完成该任务必需的上下文。需要独立核查时，提供原始依据和核查问题，避免把预期结论当作答案提示。

同一文件不分配给多个 Agent 同时修改。任务范围外的修改或新发现的问题，应返回 Main Agent 协调；不自行扩展任务或覆盖其他分支成果。

Sub-agent 可以提出科学解释、候选方案及其批评，但它们属于供整合的分析和建议，不代表人的最终决定。

## 五、结果交接与轻量核验

### Sub-agent → Main Agent

返回足以核对和继续工作的信息：当前问题、执行是否完成、关键发现、依据及产物路径、实际执行的检查与结果、假设和未决问题，以及必要的下一步或上下文更新建议。

不默认回传完整日志、所有读取内容和无关尝试。影响判断的失败和反例必须保留；关键执行状态不能只存在于临时对话中，已有项目产物可承载细节时提供其入口。不要为满足交接格式额外建立目录或登记系统。

执行状态、核验结果和科学判断分别说明，不使用单一 `PASS/FAIL` 代表整个结果。例如：

> 执行已完成；样本映射与输出一致性检查通过；两种解释仍无法区分，证据不足以回答当前问题。

未运行的检查如实标注，不能根据文件存在或报告措辞推断验证已经发生。

### Main Agent 接收与继续

Main Agent 核对返回内容是否回答委派问题，关键依据是否可定位、输入是否仍适用，以及结论是否超过检查能支持的范围。根据影响与疑点定向检查，不要求默认重跑全部工作，也不直接把 Sub-agent 的自述当成已验证事实。

输入已经改变时，先判断对结果及下游的影响；不再适用的结果保留其来源与限制，不直接合入当前有效状态。多个 Agent 的结论冲突时，比较输入、方法、假设与证据，必要时补充核查，不以多数意见代替证据。

重要任务在执行前考虑检查方式，执行中和交接时使用与问题相关的核验，例如维度与缺失检查、样本映射、统计诊断、敏感性比较或代码测试。按实际需要选择，不要求统一测试框架，也不以技术检查通过认定科学结论成立。

保留真实依赖，取消统一 Stage Gate 和逐阶段审批。缺少关键输入、验证失败或决定待定时，仅暂停依赖该条件的工作；其余已授权任务可以继续。恢复时核对所缺条件是否已满足。

## 六、人类决策与方案验收

### 方案比较和人的决定

存在会影响研究判断的合理备选方案时，先围绕当前问题明确共同输入和评价标准，比较差异、假设、局限及代价。无法使用相同输入时说明差异如何限制比较，不强行排列优劣。

不要求每个任务都制造多个候选，不统一套用一组指标，也不要求每次比较都选出唯一方案。保留多个解释、暂不选择或继续收集证据都是有效状态。

Agent 可在授权范围内完成检查、修复和分析，依照已有决定推进。新的研究方向、证据资格、边界样本取舍、主要分析方案或主结论选择，应说明依据与取舍并交由用户决定；不得把建议写成用户已批准的决定，也不对已明确授权的事项反复确认。

人的选择记录为人的选择，不能替代科学依据。用户改变决定后，更新摘要与后续任务；检查正在进行或已返回的工作是否受影响，保留仍有意义的既有观察。

### 后续重构的验收场景

| 场景 | 预期行为 |
| --- | --- |
| 普通手工项目，已有一个上下文入口 | 直接恢复工作，不创建目录、编号或记录模板 |
| 项目没有 `context.md` | 不自动创建；继续有充分依据的工作，说明哪些状态尚未持久化 |
| 新会话接管 | 通过摘要及必要链接识别当前问题、判断依据、限制和下一步 |
| 没有可用的 Sub-agent | Main Agent 能直接开展同样的工作，不因缺少委派能力阻塞 |
| 分析完成且技术检查通过，但科学结果不确定 | 分别报告完成状态、检查结果和推断限制，不宣称研究成功 |
| 用户改变研究方向 | 摘要、任务委派和后续工作随之调整，不只追加一条说明 |
| 多个 Agent 返回冲突结果 | 保留差异并核查来源，不按多数意见选择结论 |
| Sub-agent 工作期间上游输入改变 | 接收时检查适用性，不直接将旧输入结果纳入当前状态 |
| 两个独立任务中只有一个被阻塞 | 仅暂停受影响的任务，继续其余已授权工作 |
| 中断前有重要进展或不支持主解释的证据 | 保存恢复入口和有价值的观察，不压缩成单纯的成功摘要 |
| 比较后暂不选择方案 | 保留开放问题与下一步证据需求，不强迫收敛 |

这些场景检验工作行为与恢复能力，不应简化为检查标题或关键词是否存在。后续实施时分别报告实际检查过的内容与尚未验证的行为；本轮方案文档更新不代表新版 Skill 已实现或通过行为验证。
