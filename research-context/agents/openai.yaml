interface:
  display_name: "科研上下文"
  short_description: "按需恢复科研项目，将回顾按月归档，经确认维护短入口及必要主题 context"
  default_prompt: "使用 $research-context 按当前请求恢复项目、生成 AGENTS.md 或将当日/阶段回顾按月归档；仅将获准变化应用到 context，默认短总入口，必要时经确认拆分主题并按当前问题读取，历史有明确需要时再检索。"
