# 科研项目协作约定

## 工作方式

- 遵循第一性原理与奥卡姆剃刀原则；任务执行只保留必需步骤、只调用必需工具。
- 当模型为 Codex 相关模型时，限制 superpowers 的使用，并避免对无必要的小文件做 SHA256 全量审计。
- 环境管理使用 `pixi-environment-builder`；脚本编写修改使用 `scripting-style`。
- 上下文管理使用 `research-context`。
- 涉及非生物信息的实际分析的脚本编写时，新建 `temp/` 文件夹管理临时脚本。

## 上下文恢复与保存

- {{CONTEXT_ENTRY_RULE}}
- 恢复研究工作时，小型入口直接完整读取，再按当前问题读取必要资料和证据；超长内容分段读取，仅在续读或跨会话整理需要确认来源是否变化时启用指纹校验。不递归加载全部历史；输出截断或证据不可达时，不把未读取内容视为不存在。
- 入口默认预算为 4,000 个 Unicode 字符，主题记录或专题索引以 8,000 个字符为拆分参考；按实际工具限制调整，字符预算不代表模型 token 上限。
- 入口记录研究目标、当前问题、有效决定、已核实事实及限制、证据路径和下一步；详细依据保留在原产物或主题记录中。
- 重要观察、输入或决定变化后及时保存；中断、切换任务或交接前记录已完成范围、未核验部分和恢复入口。先保存依据，再更新入口；没有实质变化时不重写。
- 超长入口整理前保留可靠来源，核对重要信息的去向和引用后再替换。不得因精简上下文而删除原始证据、反例、决定理由或尚未完成的工作。
- Main Agent 维护入口和共享研究记录；Sub-agent 返回更新建议，或写入明确分配且不冲突的独立产物。

## 科研判断

- 明确研究问题、数据生成过程、比较对象和推断单位，再选择足以改变判断的最小工作；问题尚未确定时保留探索状态。
- 观察、解释与用户决定分别记录，事后发现不冒充事前设计。技术检查通过不代表假设成立；支持性、null、negative、contradictory 与 inconclusive evidence 同样保留。
- 超出既有授权的研究方向、证据资格、样本取舍、主要分析方案或主结论选择，说明依据与取舍后交由用户判断；不把建议写成已批准决定，不对已授权事项反复确认。
