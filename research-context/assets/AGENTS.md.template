# 科研项目协作约定

## 工作方式

- 遵循第一性原理与奥卡姆剃刀原则；任务执行只保留必需步骤、只调用必需工具。
- 当模型为 Codex 相关模型时，限制 superpowers 的使用，并避免对无必要的小文件做 SHA256 全量审计。
- 环境管理使用 `pixi-environment-builder`；脚本编写修改使用 `scripting-style`。
- 上下文管理使用 `research-context`。
- 仅当用户明确要求审查、精简或迭代本文件时，使用 `agents-md-maintainer`；日常任务、上下文维护或模型升级本身不触发审查或改写。
- 涉及非生物信息的实际分析的脚本编写时，新建 `temp/` 文件夹管理临时脚本。

## 上下文恢复与保存

- {{CONTEXT_ENTRY_RULE}}
- 恢复时直接完整读取短总入口，再按其中的读取条件选择当前问题的主题及必要依赖，核对必要证据；信息足够即停止，不递归加载全部主题或历史。既有超长记录定向读取并说明覆盖范围，不把截断当作读完；完整扫描仅用于明确请求的全面整理。仅在续读或跨会话整理需要核对来源变化时使用指纹校验。
- 本文件保存稳定约定；总入口保存项目目标、全局约束、当前焦点和必要路由，以 4,000 个 Unicode 字符为软目标，字符数不等于 token 上限。默认单个 context；确需拆分时，经批准增加按研究问题组织的主题 context，动态路径及读取条件只在总入口维护。各项状态只在一处权威维护，共有约束不能只放在主题中。
- 总入口和主题 context 的新增、替换、迁出及拆分共同供用户审核，获准后只应用指定变化；明确要求“把这项记入 context”已是授权，不重复确认。普通分析、交接或自动压缩不自动写入 context；确认存档不意味着永久驻留入口。
- 写入前核对依据、目标条目、内容去向及预计长度，以替换旧状态和引用细节控制增长；无实质变化时不重写。必要约束仍超预算时完整保留并说明原因，提出缩小工作范围的方案；不自动提高预算或建立主题文件，不因精简删去有效限制、拒绝理由、反面证据或原始证据。
- Main Agent 维护入口和共享研究记录；Sub-agent 返回更新建议，或写入明确分配且不冲突的独立产物。

## 科研判断

- 明确研究问题、数据生成过程、比较对象和推断单位，再选择足以改变判断的最小工作；问题尚未确定时保留探索状态。
- 观察、解释与用户决定分别记录，确认存档不代表科学结论成立；保留适用条件及待重核事项，事后发现不冒充事前设计。技术检查通过不代表假设成立；支持性、null、negative、contradictory 与 inconclusive evidence 同样保留。一次拒绝、暂缓、技术失败和科学负面结果不混用，也不自动升级为永久规则。
- 超出既有授权的研究方向、证据资格、样本取舍、主要分析方案或主结论选择，说明依据与取舍后交由用户判断；不把建议写成已批准决定，不对已授权事项反复确认。
