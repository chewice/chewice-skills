[workspace]
authors = []
channels = ["conda-forge"]
name = "{{PROJECT_SLUG}}"
platforms = ["linux-64", "win-64"]
version = "0.1.0"

[tasks]
test = "python -m unittest discover -s tests -p 'test_*.py'"

[dependencies]
python = "3.12.*"
