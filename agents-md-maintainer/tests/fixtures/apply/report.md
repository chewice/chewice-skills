# 待应用审计报告

这是一份合成的先前审计报告，审计时输入逐字保存于 `baseline.md`。下面的位置均对应该基线，不代表当前源文件仍与基线一致。

## R01 — COMPRESS

- 原文位置：`AGENTS.md:3` 与 `AGENTS.md:4`。
- 两处原文均为：`- 报告保留原始样本编号，不得丢失前导零。`
- 建议：只删除第二处相同要求，保留第一处完整原文。
- 依据：同文件、同作用域且逐字重复；没有额外条件或例外。
- 验证状态：已在审计基线静态核对等价；尚未应用。
- 预期替代文本：`- 报告保留原始样本编号，不得丢失前导零。`

```diff
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -3,5 +3,4 @@
 - 报告保留原始样本编号，不得丢失前导零。
-- 报告保留原始样本编号，不得丢失前导零。
 - 每次修改都运行全部测试。
 - 不得覆盖或删除 `data/raw/` 下的原始文件。
 - 向项目以外发布结果前，必须取得用户针对该次发布的明确批准。
```

## R02 — UPDATE

- 原文位置：`AGENTS.md:5`。
- 原文：`- 每次修改都运行全部测试。`
- 候选替代文本：`- 根据改动影响运行相关测试；涉及公共接口或分析计算逻辑时运行全部测试。`
- 依据与限制：减少无关测试可能降低执行成本，但缺少覆盖映射、历史失败场景与行为对照。
- 验证状态：未做行为验证；本项会改变检查范围，仍为待验证、未批准的候选。
