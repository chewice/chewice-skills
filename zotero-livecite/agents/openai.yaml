interface:
  display_name: "Zotero Live Cite"
  short_description: "Word live field + Zotero MCP 管库"
  default_prompt: "使用 $zotero-livecite。若只要管库：用 MCP 按 DOI/PMID 检索或入藏，不要启动 Word。若要改 Word：正文必须写成 Zotero live citation 字段，不得把编号当普通文本写入；先离线扫描，不要覆盖原稿。"
